/**
 *  Represents an action within the context of a game or simulation. Actions are the
 *  fundamental units that drive state transitions in a system.
 */
public interface Action {

    // Marker interface; no specific methods defined.
}
